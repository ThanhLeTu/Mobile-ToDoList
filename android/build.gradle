buildscript {
    repositories {
        google() // cấu hình Firebase
        mavenCentral()
    }
    dependencies {
        classpath 'com.google.gms:google-services:4.3.10' // cấu hình Firebase
    }
}

allprojects {
    repositories {
        google() // cấu hình Firebase
        mavenCentral()
    }
}

rootProject.buildDir = "../build"
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(":app")
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
